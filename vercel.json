{
  "framework": "nextjs",
  "buildCommand": "node scripts/prepare-schema.js && prisma generate && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/dolar/cron",
      "schedule": "*/10 * * * *"
    }
  ]
}
