{
  "framework": "nextjs",
  "buildCommand": "node scripts/prepare-schema.js && prisma generate && prisma db push --skip-generate && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/dolar/cron",
      "schedule": "0 12 * * *"
    }
  ]
}
