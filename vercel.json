{
  "framework": "nextjs",
  "buildCommand": "prisma db push --skip-generate --accept-data-loss && prisma generate && node prisma/seed.js && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/dolar/cron",
      "schedule": "*/10 * * * *"
    }
  ]
}
